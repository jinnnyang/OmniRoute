- **test(build):** skip symlink-collision fixture when Windows lacks symlink privilege (local fork)
- **test(mcp):** cap tool_search payload regression (SysOps F1) (local fork)
- **test(guardrails):** make the persist-usage fallback mock candidate-agnostic (local fork)
- **test(combo):** cache-protection pin regression — image turns stay on the pinned text model (local fork)
- **test(ad-hoc):** add integration-test scripts for a real Ark provider (local fork)
- **test(compression):** clean up llmlingua/SLM references after engine removal (local fork)
- **test(auto-combo):** tolerate full-suite rotation load (local fork)
- **test:** drain inherited quality snapshot reds (local fork)
- **test(ui):** reproduce invalid AnySearch icon alias (local fork)
- **test(chatcore):** move fixtures off models the lifecycle guard now rejects ([#11675](https://github.com/diegosouzapw/OmniRoute/pull/11675))
- **test(providers):** update count-derived assertions for v3.8.51 additions ([#11674](https://github.com/diegosouzapw/OmniRoute/pull/11674))
